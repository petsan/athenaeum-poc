compute:
  local_cores_min: 1
  local_cores_max: 40
memory:
  local_dram_gb: 512
  working_set_floor_gb: 16
concurrency:
  max_concurrent_active_units: 4
  question_ledger_retention: permanent
ingestion:
  disallow_paid_apis: true
